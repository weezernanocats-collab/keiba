name: Daily Results Pipeline

on:
  schedule:
    # 毎日 08:00 UTC = 17:00 JST
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  results:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Create .env.local
        run: |
          echo "TURSO_DATABASE_URL=\"${{ secrets.TURSO_DATABASE_URL }}\"" > .env.local
          echo "TURSO_AUTH_TOKEN=\"${{ secrets.TURSO_AUTH_TOKEN }}\"" >> .env.local

      - name: Run results pipeline (results + accuracy verification)
        run: npx tsx -r tsconfig-paths/register scripts/daily-pipeline.ts --job results

      - name: Cleanup
        if: always()
        run: rm -f .env.local
